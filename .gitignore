# ── Virtual environments (never push — hundreds of MB) ──────────────────────
birdclef_env/
.venv/
venv/
env/

# ── Training outputs (generated on server, not needed in repo) ──────────────
birdclef_combined/logdir/
logdir/
*.ckpt
*.pt
*.pth

# ── Dataset files (downloaded on server via setup_vast.sh) ──────────────────
*.ogg
*.wav
*.mp3
*.flac
*.h5
*.hdf5
*.parquet

# ── Inference outputs ────────────────────────────────────────────────────────
birdclef_combined/submission.csv
submission.csv

# ── Python bytecode ──────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# ── Log files ────────────────────────────────────────────────────────────────
*.log
hdf5_log.txt
train_log.txt
pipeline.log
nohup.out

# ── IDE and OS files ─────────────────────────────────────────────────────────
.vscode/
.idea/
*.DS_Store
Thumbs.db

# ── Claude Code settings (local only, never push) ───────────────────────────
.claude/

# ── Backup of trained checkpoints + logs (multi-GB, do not push) ────────────
BACKUP_FINAL/

# ── Frontend node_modules + build artifacts ─────────────────────────────────
node_modules/
app/frontend/node_modules/
app/frontend/dist/

# ── Secrets ─────────────────────────────────────────────────────────────────
.env
app/backend/.env
*.env.local

# ── Backend runtime logs ────────────────────────────────────────────────────
app/backend/backend_out.log
app/backend/backend_err.log
